export const config = {
  appName: process.env.PUBLIC_APP_NAME,
  isProd: process.env.NODE_ENV === 'production',
  // pagination: {
  //     defaultPageSize: 20,
  // },
  // features: {
  //     analytics: true
  // }
};
